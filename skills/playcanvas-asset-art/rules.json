{
  "rules": [
    {
      "id": "high-risk-engine-files",
      "description": "Warn when modifying compiled PlayCanvas engine/bundle files",
      "pattern": "(__modules__\\.js|__start__\\.js|__loading__\\.js|playcanvas-stable\\.min\\.js|viverse-auth-overlay\\.js)",
      "severity": "warning",
      "message": "This file is part of the compiled PlayCanvas engine/bundle. Read fully before editing, patch surgically, and verify syntax."
    },
    {
      "id": "valid-enum-only",
      "description": "Only use valid enum values from config.json — never invent values",
      "severity": "error",
      "message": "Read config.json assets.*.data.scripts.*.attributes.*.enum.options before setting any script attribute."
    },
    {
      "id": "verify-asset-path",
      "description": "Asset replacement path must match config.json url field exactly",
      "severity": "warning",
      "message": "Look up the asset's url in config.json before overwriting — path must be exact."
    },
    {
      "id": "image-dimensions",
      "description": "Replacement images should match original dimensions",
      "severity": "warning",
      "message": "Use sips or PIL to resize to original dimensions to avoid layout issues."
    },
    {
      "id": "no-svg-as-texture",
      "description": "SVG files cannot be used as PlayCanvas textures",
      "severity": "error",
      "message": "PlayCanvas uses WebGL textures — only PNG/JPG are valid. If you download an SVG, convert to PNG with sips before placing in files/assets/. Never rename .svg to .png."
    },
    {
      "id": "verify-patch-callsite",
      "description": "Any helper function added to __game-scripts.js must be called",
      "severity": "error",
      "message": "After patching __game-scripts.js with a new function, verify it is called in the rendering path. Search for the function name — it must appear at least twice (definition + call site). Dead code = no effect."
    },
    {
      "id": "endpoints-use-endpoint-circle",
      "description": "Start/end nodes use endpoint_circle frame, not block",
      "severity": "warning",
      "message": "If the user wants to modify start/end node visuals, patch the endpoint_circle rendering path. Blocks are obstacles — endpoints are the colored dots."
    }
  ]
}
